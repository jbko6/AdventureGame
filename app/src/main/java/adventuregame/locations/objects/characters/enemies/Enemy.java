package adventuregame.locations.objects.characters.enemies;

import adventuregame.locations.objects.characters.Character;

public class Enemy extends Character {

    public Enemy(String resourceName) {
        super(resourceName);
    }
    
}
